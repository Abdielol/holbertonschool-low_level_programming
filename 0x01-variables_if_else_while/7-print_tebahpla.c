#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the alphabet in lowercase
 *
 * Return: 0
 */
int main(void)
{
int l = 122;

while (l >= 97)
{
putchar(l);
l++;
}
putchar (10);
return (0);
}
